import type { GetAccountLinkUrlInput } from './types.js';

/** Raw zod shape for `get_account_link_url` — no inputs. */
export const inputSchema = {} as const;

export type { GetAccountLinkUrlInput };
